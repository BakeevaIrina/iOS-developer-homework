import SwiftUI

/*
 TASK 0:
 Создайте переменную, в которой будете хранить булево значение. Далее напишите код, который будет выводить в консоль "1", если значение true и "0", если значение false.
 */

var boolNumber = false

if boolNumber == true {
    print(1)
} else {
    print(0)
}

/*
 TASK 1:
 Создайте переменную, в которой будете хранить целочисленное значение. Затем напишите код, который будет выводить в консоль ваше значение с противоположным знаком. Используя знания об операторах придумайте как минимум 3 разных способа поменять знак у вашего значения.
 */


var number: Int = 5
let numberOne = number - number * 2
let numberTwo = number * (-1)
let numberThree = 0 - number
print(numberOne, numberTwo, numberThree)
/*
 TASK 2:
 Создайте переменную, в которой будете хранить значение календарного года (1999 или 1800 и тд). Придумайте логику, которая будет вычислять номер века, к которому относиться введенный год. Примите отрицательные значения, как значение года до нашей эры. Максимально лаконично выведите в консоль сообщение вроде "-132 is the 2 century BC" или "1918 is the 20 century AD"
 */

let date = -132
var century = date / 100

if date >= 0 {
    century += 1
    print("\(date) is the \(century) century AD")
} else {
    century = (century - 1) * (-1)
    print("\(date) is the \(century) century BC")
}
